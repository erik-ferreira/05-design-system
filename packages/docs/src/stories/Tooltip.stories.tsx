import { Meta, StoryObj } from "@storybook/react";
import { Tooltip, TooltipProps } from "@erik-ignite-ui/react";

export default {
  title: "Overlay/Tooltip",
  component: Tooltip,
  args: {},
} as Meta<TooltipProps>;

export const Primary: StoryObj<TooltipProps> = {};
